# Diretorio s4_patch_update
<Directory "${DOCROOT_HTTP}/S4_Patch/update/">

    Options FollowSymLinks
    
    <FilesMatch ".*">
        require all denied
    </FilesMatch>

    <FilesMatch "\.zip$">
        require all granted
    </FilesMatch>

 </Directory>